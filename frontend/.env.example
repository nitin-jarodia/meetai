# Public URL of the FastAPI backend (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000
# If you use https in production, WebSocket URLs use wss:// automatically (see lib/publicApi.ts).
# If you open the app via LAN IP, set this to http://YOUR_IP:8000 and add the matching origin to backend CORS_ORIGINS.
